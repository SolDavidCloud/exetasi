{
  "name": "exetasi",
  "version": "0.0.1",
  "description": "Free quiz and exam webapp (monorepo root)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --dir frontend dev",
    "build": "pnpm --dir frontend build",
    "lint": "pnpm --dir frontend lint",
    "format": "pnpm --dir frontend format",
    "test": "pnpm --dir frontend test",
    "test:e2e": "pnpm --dir frontend test:e2e",
    "codegen": "pnpm --dir frontend codegen"
  },
  "engines": {
    "node": "^28 || ^26 || ^24 || ^22.12",
    "pnpm": ">= 10.0.0"
  },
  "pnpm": {
    "overrides": {
      "chokidar": "^5.0.0",
      "undici-types": "6.24.0"
    }
  }
}
