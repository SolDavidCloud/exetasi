---
description: Core project context for Exetasi — stack, commands, and conventions
alwaysApply: true
---

# Exetasi Project Overview

Quiz and exam SPA. Full spec in `Requirements.md`.

## Stack
- Frontend: **Quasar 2** (Vue 3 / TypeScript), hash-mode SPA + Capacitor mobile
- Backend: **FastAPI** (Python) — separate project, communicates via REST
- Database: **PostgreSQL**
- State: **Pinia** | Routing: **Vue Router 4** | i18n: **vue-i18n 11**

## Commands
```bash
pnpm dev      # dev server
pnpm build    # production build
pnpm lint     # ESLint
pnpm format   # Prettier
```

## Non-Negotiable Conventions
- **pnpm only** — never suggest npm or yarn commands
- **`<script setup lang="ts">`** in every SFC — no Options API
- **All UI strings through `t('key')`** — no hardcoded copy anywhere
- **Strict TypeScript** — no `any`, no unsafe `as` casts
- **Quasar components** (`QBtn`, `QInput`, `QCard`…) over native HTML equivalents
